% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_model.R
\name{fit_cox_model_from_files}
\alias{fit_cox_model_from_files}
\title{Fit Cox Model from Files}
\usage{
fit_cox_model_from_files(
  phenotype_file,
  covariate_file,
  output_file,
  use_spacox = TRUE
)
}
\arguments{
\item{phenotype_file}{Path to CSV file with columns: IID, time, status}

\item{covariate_file}{Path to CSV file with columns: IID, covar1, covar2, ...}

\item{output_file}{Path to RDS file to save the fitted null model}

\item{use_spacox}{Whether to try using SPACox package (default: TRUE)}
}
\value{
Invisible path to the output file
}
\description{
Fits a null Cox model by reading phenotype and covariate data from files.
This function is designed for batch processing and large-scale analysis
where data is stored in separate files.
}
\examples{
\dontrun{
# Prepare example data files
pheno_data <- data.frame(
  IID = paste0("ID", 1:100),
  time = rexp(100, 0.1),
  status = rbinom(100, 1, 0.3)
)
covar_data <- data.frame(
  IID = paste0("ID", 1:100),
  age = rnorm(100, 50, 10),
  sex = rbinom(100, 1, 0.5)
)

write.csv(pheno_data, "phenotype.csv", row.names = FALSE)
write.csv(covar_data, "covariates.csv", row.names = FALSE)

# Fit model from files
fit_cox_model_from_files(
  phenotype_file = "phenotype.csv",
  covariate_file = "covariates.csv", 
  output_file = "null_model.rds"
)

# Load the fitted model
model_info <- readRDS("null_model.rds")
}
}
\keyword{internal}
